#first I need to Downland and install the library "INFLECT"
"pip isntall inflect"
import inflect
def main():

    name = []
    p = inflect.engine() #now im assigning the module to a variable


    while True:
        name = input("Name: ")
       
        
        choice = input("another: ")
        if choice == 'end':
            break
        
        
    print(f"Adieu, adeiu, to {name} and {name[]}")   


main()


